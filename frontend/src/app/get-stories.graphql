fragment BaseStoryFragment on Story {
    id
    title
    excerpt(maxLength: 150)
    date
    category
    likes
    image { uri }
    wordCount
    writer {
        name
    }
}
query GetStories {
    stories(pageSize: 16) {
        results {
            ...BaseStoryFragment
        }
    }
}